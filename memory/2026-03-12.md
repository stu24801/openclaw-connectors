# 2026-03-12

## RAG Knowledge Base 修復與更新
- 修正了文章頁面對話框無法載入的問題：將 `fetch()` 補上 `credentials: 'include'`，讓瀏覽器正確攜帶 Cookie 送出請求。
- 修正前端連點/Enter 雙觸發問題：在回饋輸入框加入 `_sending` lock guard。
- 解決 agent-to-agent 跨 session 通訊阻擋：在 `openclaw.json` 開啟 `tools.sessions.visibility = all` 及 `tools.agentToAgent.enabled = true`。
- 修改文章回饋流程：改由 RAG Server 在背景等待寫手蝦（LLM）處理，如果寫手蝦輸出 `<revised_article>...</revised_article>` 標籤包覆的內容，系統會自動儲存為新版本，並在前端提供版本切換。
- 實作了自動化部署腳本 `/opt/rag-kb/deploy.sh` 方便未來一鍵重啟更新。
## Banana Slides - PPTX 匯出改良（PaddleOCR + outline matching）

### 完成項目
1. **`edit_image` 背景去字**：`task_manager.py` ~line 515 改用 `ai_service.edit_image()` + 英文 prompt 清除投影片文字，保留原版面配置。英文 prompt 效果明顯優於中文 prompt。

2. **PaddleOCR 安裝**：
   - `backend/Dockerfile` 新增 `libgl1` 和 `libglib2.0-0`（Debian trixie 上要用 `libgl1` 而非 `libgl1-mesa-glx`）
   - `pyproject.toml` 新增 `paddlepaddle>=2.6.1` 和 `paddleocr>=2.8.0`
   - 確認安裝成功：`paddleocr==3.4.0`，`paddlepaddle==3.3.0`

3. **`detect_text_regions_ocr()` 函數**：新增在 `task_manager.py` line 22（舊 VLM 函數前）。輸出格式與原 `detect_text_regions` 相容：`[{text, x0, y0, x1, y1, type, color}]`，座標為 0~1 比例。使用全域 `_ocr_instance` 單例避免重複載入模型。

4. **`export_service.py` 改良**：
   - 加入 `_match_outline_to_regions(title, points, text_regions)` 內部函數：用 `difflib.SequenceMatcher` 將 OCR bbox 與 `outline_content` 的 title/points 配對，配對結果以 outline 正確文字為準
   - 模式 A：有 `text_regions` → OCR bbox + outline text → 每個 point 各自獨立文字框
   - 模式 B：fallback，`text_regions` 為空或匹配失敗 → 固定位置文字框
   - 整合點：模式 A 的 `title` 標記 `type='title'`，`points` 各標記 `type='bullet'`，文字顏色從 OCR 繼承或預設

5. **Docker 重建並重啟**：新容器已成功運行，PaddleOCR 可正常 import。

### 重要設定與路徑
- 後端服務：`banana-slides-backend` 容器，source mount `/opt/banana-slides/backend/` → `/app/backend/`
- `detect_text_regions_ocr` 在 `task_manager.py` line 22，呼叫點在 line 615
- OCR 信心閾值：0.6；未達閾值的結果被丟棄
- OCR 預設 `type: "other"`，`color: "#FFFFFF"`

### 下一步（未完成）
- 端對端測試：上傳新投影片，觸發處理，確認 `text_regions` 被 OCR 正確填入，PPTX 匯出正常
- commit & push：`backend/Dockerfile`、`pyproject.toml`、`backend/services/task_manager.py`、`backend/services/export_service.py`
